version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"        # safe choice for RTD images
  jobs:
    pre_build:
      - python generate_proto.py
sphinx:
  configuration: docs/conf.py
python:
  install:
    - requirements: docs/requirements.txt
    - requirements: requirements.txt   # your project deps so autodoc imports succeed
